{
  "name": "polish-history-quest",
  "version": "0.7.0",
  "private": false,
  "type": "module",
  "description": "A browser-based interactive learning game for adults learning Polish history.",
  "scripts": {
    "dev": "bunx --bun vite --host 0.0.0.0",
    "build": "bunx --bun vite build && bun scripts/verify-built-static.mjs",
    "preview": "bunx --bun vite preview --host 0.0.0.0",
    "test": "bun scripts/verify-content.mjs && bun scripts/verify-curriculum.mjs && bun scripts/verify-companion.mjs && bun scripts/verify-progress.mjs && bun scripts/verify-learning-ui.mjs && bun scripts/verify-games.mjs",
    "check:media": "bun scripts/check-media-links.mjs && bun scripts/check-companion-media.mjs"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "keywords": [
    "polish-history",
    "education",
    "static-site",
    "game",
    "vite"
  ],
  "license": "MIT",
  "devDependencies": {
    "@vitejs/plugin-legacy": "latest",
    "vite": "latest",
    "yaml": "^2.9.0"
  },
  "trustedDependencies": [
    "core-js"
  ]
}
